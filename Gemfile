source "https://rubygems.org"

# GitHub Pages builds the site with this gem set; keeping the same set locally
# means `bundle exec jekyll serve` matches what GitHub renders.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
gem "csv"
gem "base64"
gem "bigdecimal"
